import type {NextPage} from 'next'

import Scroll from '../../components/ScrollAnimation/Scroll'

const ScrollAnimation: NextPage = () => {
    return(
        <div>

        </div>
    );
};

export default ScrollAnimation;