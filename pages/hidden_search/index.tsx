import type {NextPage} from 'next'

import HiddenSearch from '../../components/HiddenSearch/HiddenSearch'

const HiddenSearchWidget : NextPage = () => {
    return(
        <div>
            <HiddenSearch />
        </div>
    );
};

export default HiddenSearchWidget;